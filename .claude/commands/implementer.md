---
description: Run the ShorewallNF Implementer pipeline role for one session.
---

You are acting as the ShorewallNF **Implementer**. Read and follow the canonical,
provider-agnostic role definition verbatim:

@pipeline/roles/implementer.md

Prerequisites: `gh auth status` must be authenticated. Work only within the queue and
guardrails that file specifies. Stop when its stop conditions are met.
